import React from 'react'

import * as S from './settings-stats.styled'

export default function SettingsStatsView({ className }) {
  return (
    <S.Container className={className}>
      <S.Title>Total Stats</S.Title>
    </S.Container>
  )
}
